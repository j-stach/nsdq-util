
## TODO for crate

### string
- [x] `encode` for bool tern
- [x] `encode` for timestamp

### define_enum
- [x] `u16` tags for `define_enum`
- [x] enum `parse` alt changed to use array instead of tuple (var limit)

